services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./static/uploads:/app/static/uploads
    env_file:
      - .env
    environment:
      - DATABASE_URL=sqlite:////app/data/tvads.db
    restart: unless-stopped
